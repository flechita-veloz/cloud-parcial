{{- if not (lookup "v1" "PersistentVolumeClaim" .Release.Namespace "postgres-pvc") }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: inventory
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: gp2
{{- end }}